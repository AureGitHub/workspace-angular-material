export enum MaterialButtonColor {
  Primary = 'primary',
  Accent = 'accent',
  Warn = 'warn'
}

export enum MaterialButtonIcon {
  none = '',
  save = 'save',  
  help= 'help',     
  cancel= 'cancel',  
  volver = 'arrow_back',
  info = 'info'
}

export const slEnum = {
    color : MaterialButtonColor,
    icon : MaterialButtonIcon
}

